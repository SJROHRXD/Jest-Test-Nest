const numbers = [-3, -2, -1, 0, 1, 2, 3];

// Filter Array
const filteredNumbers = numbers.filter(n => n >= 0);

// console.log(filteredNumbers);
// output [0, 1, 2, 3]

// my dumbass forgot to export
module.exports = filteredNumbers;